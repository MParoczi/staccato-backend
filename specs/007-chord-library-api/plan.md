# Implementation Plan: Chord Library API

**Branch**: `007-chord-library-api` | **Date**: 2026-03-28 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/007-chord-library-api/spec.md`

## Summary

Expose three public read-only endpoints — `GET /instruments`, `GET /chords`, `GET /chords/{id}` — backed by seeded, immutable data. Requires adding `Root`, `Quality`, `Extension`, and `Alternation` columns to `ChordEntity`, dropping the `Suffix` column, using a 13-value quality taxonomy, replacing the file-path-based seeder with an embedded-resource differential seeder that reads `name`/`root`/`quality`/`extension`/`alternation` directly from the transformed JSON, wiring a new `ChordService` and `InstrumentService` through the standard Service / Repository / UoW stack, and adding response DTOs + AutoMapper profiles. No new libraries.

---

## Technical Context

**Language/Version**: C# 13, .NET 10
**Primary Dependencies**: ASP.NET Core 10, Entity Framework Core 10, AutoMapper, FluentValidation, xUnit + Moq
**Storage**: SQL Server via EF Core; `guitar_chords.json` embedded resource in Persistence assembly
**Testing**: xUnit unit tests (Moq); integration tests (WebApplicationFactory + InMemory EF)
**Target Platform**: ASP.NET Core 10 web service
**Project Type**: Web service — 9-project clean-architecture solution
**Performance Goals**: Filtered chord queries perceptibly instant; 5-minute response caching on read-only public endpoints
**Constraints**: No new libraries; Domain references DomainModels only; repositories never call SaveChanges

---

## Constitution Check

| # | Gate | Status | Notes |
|---|---|---|---|
| G1 | Project references match the dependency map. Domain references DomainModels only. | ✅ | All new code follows existing dependency map. `ChordService` references only `DomainModels` interfaces and models. |
| G2 | No controller contains business logic; all business logic lives in a Domain service. | ✅ | `ChordsController` and `InstrumentsController` are thin: extract params → call service → map → return. Instrument existence validation is in `ChordService`. |
| G3 | All repository methods omit `SaveChanges`; persistence committed via `IUnitOfWork.CommitAsync` only. | ✅ | Chord and instrument data are read-only at runtime. `ChordSeeder` calls `SaveChangesAsync` directly — this is acceptable for seeders, which are infrastructure-layer startup code, not service-layer operations. |
| G4 | Business rule violations thrown as typed `BusinessException` subclasses — no `IActionResult` from services. | ✅ | `ChordService` throws `NotFoundException` for unknown chord or instrument. |
| G5 | All primary keys are `Guid` generated by the application; no DB-generated keys. | ✅ | `ChordSeeder` uses `Guid.NewGuid()`. Existing keys are preserved by differential seeding. |
| G6 | Cascade delete rules match hierarchy; Chord and Instrument use `DeleteBehavior.Restrict`. | ✅ | `ChordConfiguration` already has `OnDelete(DeleteBehavior.Restrict)`. No change needed. |
| G7 | HTTP status codes, error envelope format, and ownership-check behaviour match API Contract Discipline. | ✅ | `GET` returns 200; `404` via `NotFoundException` through `BusinessExceptionMiddleware`; 400 via FluentValidation/model binding. |
| G8 | All user text stored as `TextSpan[]` (bold only, no other formatting). | ✅ | N/A — no user text storage in this feature. |
| G9 | `ModuleTypeConstraints` and `PageSizeDimensions` are the only source of truth for blocks and grid. | ✅ | N/A — no module or grid changes. |
| G10 | No secret hardcoded; all config via `IOptions<T>`; tokens stored per security rules. | ✅ | No secrets in this feature. |
| G11 | Unit tests cover happy path + all exception paths; integration tests cover all new endpoints. | ✅ | `ChordServiceTests` (happy + NotFoundException paths), `InstrumentServiceTests`, `ChordsControllerTests`, `InstrumentsControllerTests` planned. Existing seeder tests updated. |
| G12 | Nullable enabled; no `.Result`/`.Wait()`; `CancellationToken` threaded through all async methods. | ✅ | All new async methods accept `CancellationToken ct = default`. |
| G13 | No new library introduced outside the approved technology stack. | ✅ | Response caching uses ASP.NET Core built-in middleware. No new NuGet packages. |

---

## Project Structure

### Documentation (this feature)

```text
specs/007-chord-library-api/
├── plan.md                    ← this file
├── research.md                ← Phase 0 output
├── data-model.md              ← Phase 1 output
├── contracts/endpoints.md     ← Phase 1 output
└── tasks.md                   ← Phase 2 output (/speckit.tasks)
```

### Source Code Changes

```text
Staccato.sln
├── Application/
│   ├── Extensions/ServiceCollectionExtensions.cs   [MODIFY] add IChordService + IInstrumentService
│   └── Program.cs                                  [MODIFY] add response caching middleware
│
├── Api/
│   ├── Controllers/
│   │   ├── ChordsController.cs                     [NEW]
│   │   └── InstrumentsController.cs                [NEW]
│   └── Mapping/
│       └── DomainToResponseProfile.cs              [MODIFY] add chord + instrument mappings
│
├── Domain/
│   ├── Interfaces/Repositories/
│   │   └── IInstrumentRepository.cs                [MODIFY] add GetByKeyAsync(InstrumentKey, ct)
│   └── Services/
│       ├── IChordService.cs                        [NEW]
│       ├── ChordService.cs                         [NEW]
│       ├── IInstrumentService.cs                   [NEW]
│       └── InstrumentService.cs                    [NEW]
│
├── Repository/
│   ├── Mapping/EntityToDomainProfile.cs            [MODIFY] chord mapping: add InstrumentKey + Positions
│   └── Repositories/
│       ├── ChordRepository.cs                      [MODIFY] fix filters, add Include, add ordering
│       └── InstrumentRepository.cs                 [MODIFY] implement GetByKeyAsync
│
├── Persistence/
│   ├── Configurations/ChordConfiguration.cs        [MODIFY] add Root/Quality + composite index
│   ├── Seed/ChordSeeder.cs                         [MODIFY] embedded resource + differential + Root/Quality
│   ├── Migrations/[AddChordRootAndQuality]         [NEW] adds Root, Quality cols + composite index
│   └── Persistence.csproj                          [MODIFY] EmbeddedResource for guitar_chords.json
│
├── EntityModels/
│   └── Entities/ChordEntity.cs                     [MODIFY] add Root + Quality properties
│
├── DomainModels/
│   └── Models/
│       ├── Chord.cs                                [MODIFY] add Root, Quality, InstrumentKey, Positions
│       ├── ChordPosition.cs                        [NEW]
│       ├── ChordBarre.cs                           [NEW]
│       └── ChordString.cs                          [NEW]
│
├── ApiModels/
│   ├── Chords/
│   │   ├── ChordSummaryResponse.cs                 [NEW]
│   │   ├── ChordDetailResponse.cs                  [NEW]
│   │   ├── ChordPositionResponse.cs                [NEW]
│   │   ├── ChordBarreResponse.cs                   [NEW]
│   │   └── ChordStringResponse.cs                  [NEW]
│   └── Instruments/
│       └── InstrumentResponse.cs                   [NEW]
│
└── Tests/
    ├── Unit/
    │   ├── Services/
    │   │   ├── ChordServiceTests.cs                [NEW]
    │   │   └── InstrumentServiceTests.cs           [NEW]
    │   ├── Mapping/DomainToResponseProfileTests.cs [MODIFY] add chord + instrument mapping tests
    │   └── Persistence/
    │       ├── ChordSeederHappyPathTests.cs         [MODIFY] update for embedded resource + Name change
    │       └── ChordSeederFailTests.cs              [MODIFY] update testable seeder override mechanism
    └── Integration/
        └── Controllers/
            ├── ChordsControllerTests.cs            [NEW]
            └── InstrumentsControllerTests.cs       [NEW]
```

---

## Complexity Tracking

No constitution violations. No entries needed.

---

## Implementation Sequence

The tasks below are ordered by dependency. Each task is independently committable.

### Group A — Data Model Foundation (no dependencies)

**A1** — Add `Root` and `Quality` to `ChordEntity`
- `EntityModels/Entities/ChordEntity.cs`: add `public string Root { get; set; } = string.Empty;` and `public string Quality { get; set; } = string.Empty;`

**A2** — Add new domain models
- `DomainModels/Models/ChordPosition.cs`: `Label`, `BaseFret`, `Barre?`, `Strings`
- `DomainModels/Models/ChordBarre.cs`: `Fret`, `FromString`, `ToString`
- `DomainModels/Models/ChordString.cs`: `StringNumber`, `State`, `Fret?`, `Finger?`

**A3** — Update `Chord` domain model
- `DomainModels/Models/Chord.cs`: remove `PositionsJson`; add `Root`, `Quality`, `InstrumentKey`, `Positions: List<ChordPosition>`

### Group B — Persistence (depends on A1)

**B1** — Update `ChordConfiguration`
- Remove `Suffix` column config
- Add `Root` column config (`IsRequired().HasMaxLength(50)`)
- Add `Quality` column config (`IsRequired().HasMaxLength(50)`)
- Add `Extension` column config (`.HasMaxLength(50)` — nullable, no `IsRequired()`)
- Add `Alternation` column config (`.HasMaxLength(50)` — nullable, no `IsRequired()`)
- Add composite index on `(InstrumentId, Root, Quality)`

**B2** — Add EF Core migration `RestructureChordSchema`
- `dotnet ef migrations add RestructureChordSchema`
- Edit generated migration: add `migrationBuilder.Sql(...)` for data population
- See data-model.md §EF Core Migration for the full SQL: adds Root/Quality/Extension/Alternation columns, populates from old Suffix values via CASE-WHEN, updates Name to display name, then drops the Suffix column

**B3** — Update `ChordSeeder` — embedded resource + differential + new schema
- Change `Persistence.csproj`: `<Content>` → `<EmbeddedResource>` for `guitar_chords.json`
- Change access from file path to `Assembly.GetManifestResourceStream`
- Change `virtual string ChordFilePath` → `virtual Stream? GetChordStream()` override pattern
- Update `ChordRecord` private DTO: fields `Name`, `Root`, `Quality`, `Extension?`, `Alternation?` (all read directly from JSON; no derivation)
- Map JSON directly: `Name = r.Name`, `Root = r.Root`, `Quality = r.Quality`, `Extension = r.Extension`, `Alternation = r.Alternation`
- Remove `Suffix` from `ChordRecord` and `ChordEntity` assignment — it no longer exists
- Replace skip-if-any with differential: load existing `(InstrumentId, Root, Quality, Extension ?? "")` tuples as HashSet, insert only those absent
- Note: for Major chords `Extension` is null and `Name` equals the root note letter (e.g. `"C"` for C major)

**B4** — Update `InstrumentSeeder` — differential seeding (required by FR-009)
- Currently uses skip-if-any; change to per-record differential to match spec
- Natural key: `Key` enum value
- Load existing instrument `Key` values into a HashSet; insert only those absent

### Group C — Repository Layer (depends on A1, A2, A3)

**C1** — Update `EntityToDomainProfile`
- Replace `CreateMap<ChordEntity, Chord>().ReverseMap()` with explicit mapping:
  - `ChordEntity.Instrument.Key → Chord.InstrumentKey`
  - `ChordEntity.PositionsJson → Chord.Positions` via `JsonSerializer.Deserialize<List<ChordPosition>>`
  - Handle BOM-safe deserialization; use `PropertyNameCaseInsensitive = true`
- Add mappings for `ChordPosition`, `ChordBarre`, `ChordString` (if needed; may be handled inline)

**C2** — Update `ChordRepository`
- `SearchAsync`: fix `c.Name == root` → `c.Root.ToLower() == root.ToLower()` (case-insensitive); fix `c.Suffix == quality` → `c.Quality.ToLower() == quality.ToLower()`; add `.Include(c => c.Instrument)`; add `.OrderBy(c => c.Root).ThenBy(c => c.Quality)`
- Override `GetByIdAsync`: add `.Include(c => c.Instrument)` (base class doesn't include navigation properties)

**C3** — Update `IInstrumentRepository` + `InstrumentRepository`
- `IInstrumentRepository`: add `Task<Instrument?> GetByKeyAsync(InstrumentKey key, CancellationToken ct = default)`
- `InstrumentRepository`: implement `.Where(i => i.Key == key).AsNoTracking().FirstOrDefaultAsync(ct)`, then map

### Group D — API Models (no dependencies)

**D1** — Create response DTOs in `ApiModels`
- `ApiModels/Instruments/InstrumentResponse.cs` — record with `Id, Key, Name, StringCount`
- `ApiModels/Chords/ChordSummaryResponse.cs` — record with `Id, InstrumentKey, Name, Root, Quality, Extension?, Alternation?, PreviewPosition` (no `Suffix`)
- `ApiModels/Chords/ChordDetailResponse.cs` — record with `Id, InstrumentKey, Name, Root, Quality, Extension?, Alternation?, Positions` (no `Suffix`)
- `ApiModels/Chords/ChordPositionResponse.cs` — record with `Label, BaseFret, Barre?, Strings`
- `ApiModels/Chords/ChordBarreResponse.cs` — record with `Fret, FromString, ToString`
- `ApiModels/Chords/ChordStringResponse.cs` — record with `String, State, Fret?, Finger?`
- All records; no validators needed (all responses, no requests)
- Note: `ChordStringResponse.String` needs `[JsonPropertyName("string")]` since `string` is a C# keyword

### Group E — Domain Services (depends on A3, C3)

**E1** — Create `IInstrumentService` + `InstrumentService`
- `Domain/Services/IInstrumentService.cs`: `Task<IReadOnlyList<Instrument>> GetAllAsync(CancellationToken ct = default)`
- `Domain/Services/InstrumentService.cs`: inject `IInstrumentRepository`; delegate to `GetAllAsync`

**E2** — Create `IChordService` + `ChordService`
- `Domain/Services/IChordService.cs`:
  - `Task<IReadOnlyList<Chord>> SearchAsync(InstrumentKey instrumentKey, string? root, string? quality, CancellationToken ct = default)`
  - `Task<Chord> GetByIdAsync(Guid id, CancellationToken ct = default)`
- `Domain/Services/ChordService.cs`:
  - `SearchAsync`: call `IInstrumentRepository.GetByKeyAsync(key, ct)` → if null throw `NotFoundException` with code `INSTRUMENT_NOT_FOUND`; then call `IChordRepository.SearchAsync(instrument.Id, root, quality, ct)`
  - `GetByIdAsync`: call `IChordRepository.GetByIdAsync(id, ct)` → if null throw `NotFoundException`

### Group F — AutoMapper Response Profiles (depends on A2, A3, D1)

**F1** — Update `DomainToResponseProfile`
- `Instrument → InstrumentResponse`: map `DisplayName → Name`, `Key.ToString() → Key`
- `Chord → ChordSummaryResponse`: map `InstrumentKey.ToString() → InstrumentKey`, `Positions[0] → PreviewPosition`, `Extension → Extension` (nullable pass-through), `Alternation → Alternation` (nullable pass-through)
- `Chord → ChordDetailResponse`: map `InstrumentKey.ToString() → InstrumentKey`, `Positions → Positions`, same nullable pass-throughs
- `ChordPosition → ChordPositionResponse`: straightforward; `Barre?` mapped if not null
- `ChordBarre → ChordBarreResponse`: straightforward
- `ChordString → ChordStringResponse`: map `StringNumber → String`, `State.ToString().ToLower() → State`
- No `Suffix` mapping — not present in domain model or response DTOs

### Group G — Controllers (depends on D1, E1, E2, F1)

**G1** — Create `InstrumentsController`
- Route: `[Route("api/instruments")]`
- `GET /instruments`: no `[Authorize]`; call `IInstrumentService.GetAllAsync`; map to `IReadOnlyList<InstrumentResponse>`; return `Ok(result)`

**G2** — Create `ChordsController`
- Route: `[Route("api/chords")]`
- `GET /chords?instrument=&root=&quality=`: no `[Authorize]`; `instrument` bound as `InstrumentKey` enum (model binding handles 400 for invalid values); `[FromQuery] InstrumentKey instrument` — required via `[Required]` or FluentValidation; `[ResponseCache(Duration = 300, Location = ResponseCacheLocation.Any)]`
- `GET /chords/{id}`: no `[Authorize]`; `[ResponseCache(Duration = 300, Location = ResponseCacheLocation.Any)]`

**Note on `instrument` validation**: model binding to `InstrumentKey` enum will return a model-state error (400) automatically when the value is not a valid enum member. No additional FluentValidation validator needed for a simple enum-bound query param. If the param is absent, binding will fail with a required-field validation error.

### Group H — DI Registration + Middleware (depends on E1, E2)

**H1** — Register services in `ServiceCollectionExtensions.AddDomainServices()`
- `services.AddScoped<IChordService, ChordService>()`
- `services.AddScoped<IInstrumentService, InstrumentService>()`

**H2** — Add response caching to `ServiceCollectionExtensions` and `Program.cs`
- `ServiceCollectionExtensions`: add `services.AddResponseCaching()`
- `Program.cs`: add `app.UseResponseCaching()` immediately before `app.UseAuthentication()`

### Group I — Tests (depends on E1, E2, G1, G2)

**I1** — Update `ChordSeederHappyPathTests`
- Update `TestableChordSeeder` — override `GetChordStream()` instead of `ChordFilePath`
- Update test JSON payloads to use new format (`name`, `root`, `quality`, `extension`, `alternation` fields — no `suffix`)
- Existing `chord.Name == "C"` assertion remains correct for C major (quality is `"Major"`, extension is null, so name = root letter = "C")
- Add test: `SeedAsync_PartiallySeeded_InsertsOnlyNewChords` — seeds 2 chords, calls again with 3, verifies total = 3

**I2** — Update `ChordSeederFailTests`
- Update `TestableChordSeeder` override mechanism (stream instead of file path)
- Remove tests for "file not found" and "failed to read file" (no longer applicable with embedded resource)
- Keep: invalid JSON, null/empty array, missing fields, empty positions, duplicate natural key

**I3** — Create `ChordServiceTests`
- Happy path: `SearchAsync` returns filtered list
- Exception path: `SearchAsync` with unknown instrument key throws `NotFoundException`
- Exception path: `GetByIdAsync` with unknown id throws `NotFoundException`
- Happy path: `GetByIdAsync` returns chord with positions

**I4** — Create `InstrumentServiceTests`
- Happy path: `GetAllAsync` returns all instruments from repository

**I5** — Create `ChordsControllerTests`
- `GET /chords?instrument=Guitar6String` → 200 with chord summaries
- `GET /chords?instrument=Guitar6String&root=A&quality=major` → 200 with filtered results
- `GET /chords` (no instrument param) → 400
- `GET /chords?instrument=Invalid` → 400
- `GET /chords/{id}` with valid ID → 200 with full positions
- `GET /chords/{id}` with unknown ID → 404

**I6** — Create `InstrumentsControllerTests`
- `GET /instruments` → 200 with all instruments
- `GET /instruments` without auth header → 200 (public endpoint)

---

## Risk Register

| Risk | Likelihood | Mitigation |
|---|---|---|
| BOM in guitar_chords.json causes JSON parse failure | **High** — file has UTF-8 BOM | Use `new StreamReader(stream)` which strips BOM automatically, not `Stream.Read` directly |
| Existing seeder tests break after embedded-resource change | **High** | Override mechanism changes from file path to stream; tests updated in I1/I2 |
| `Name` semantic change breaks existing assertions | **Medium** | For `Major` quality chords `chord.Name` is still the root letter (e.g. `"C"`), so the existing `chord.Name == "C"` assertion in `ChordSeederHappyPathTests` remains correct if the test uses a Major chord. All other quality assertions are affected (e.g. minor chord name is now `"Cm"` not `"C"`). |
| `Suffix` column removed — any code referencing `ChordEntity.Suffix` fails to compile | **High** | Search for all `Suffix` references across EntityModels, DomainModels, Repository, and ApiModels before applying the migration. Seeder tests using JSON payloads must switch to the new JSON format (no `suffix` field). |
| Response caching caches error responses | Low | ASP.NET Core only caches successful (200) responses by default |
| AutoMapper `Positions.First()` throws if chord has no positions | Low | Seeder validates `Positions.Count > 0` before inserting; all seeded chords guaranteed to have at least one position |
| Migration data-population step fails on empty Chords table | Low — not an error | `WHERE Root = ''` will match zero rows on fresh DB; safe no-op |
